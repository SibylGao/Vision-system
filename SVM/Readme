
------------------------------------
libsvm的使用详见README文档，要先用makefile编译库和可执行文件
SVM程序：
1.将图片改为libsvm需要的格式，分为训练集和测试集。训练集与测试集的比大于4：1
（libsvm的头文件，动态库，可执行cpp文件要拷贝到项目工程下。）

2.在终端里将模型训练出来，语法见README文档：
./svm-train -s 1 -t 1 -d 2 -g 5 -r 2 -c 1 -n 0.1 -b 1 ../skyer_data/trainlog_1

3.将模型导入cpp工程，并计算正确率
（这里在终端跑出来正确率在99.6%左右，在程序里同一个模型却识别不出来数字，但程序是跑得通，这里应该没有什么问题）


参考资料：
1.libsvm的README文档
2.吴恩达机器学习课程SVM原理部分
